/** Design philosophy: Field Notes Utility — credible, compact Android-inspired content cards rather than speculative dashboards. */

import { Copy, Folder, Hash, MapPin, MoreHorizontal, Search, Star } from "lucide-react";
type PhoneMockupProps = { variant?: "recent" | "reference" | "folders"; className?: string };
const recentCards = [{ label: "Text", title: "Useful details for later", meta: "Local · just now" }, { label: "URL", title: "Reference preview", meta: "Saved link" }, { label: "Hashtag", title: "#workflows #notes", meta: "Extracted reference" }];
export default function PhoneMockup({ variant = "recent", className = "" }: PhoneMockupProps) { return <div className={`phone-mockup ${className}`} aria-label={`Mr. Copy ${variant} interface example`}><div className="phone-speaker" aria-hidden="true" /><div className="phone-screen"><div className="phone-status"><span>9:41</span><span>•••</span></div><div className="mockup-topbar"><strong>Mr. Copy</strong><Search size={15} aria-hidden="true" /></div>{variant === "recent" && <><div className="mockup-tabs"><span className="active">Recent</span><span>Starred</span><span>Folders</span></div><div className="mockup-card-list">{recentCards.map((card, index) => <div className="mockup-content-card" key={card.label}><div className="mockup-card-top"><small>{card.label}</small>{index === 0 ? <Star size={12} aria-hidden="true" /> : <MoreHorizontal size={13} aria-hidden="true" />}</div><strong>{card.title}</strong><span>{card.meta}</span></div>)}</div><button type="button" className="mockup-copy-action"><Copy size={14} aria-hidden="true" /> One-Tap Copy</button></>}{variant === "reference" && <><div className="mockup-back">‹ Reference</div><div className="mockup-preview-image"><span>Preview</span></div><h3 className="mockup-reference-title">A useful page to revisit</h3><div className="mockup-meta-row"><span><Hash size={12} aria-hidden="true" /> Hashtag</span><span><MapPin size={12} aria-hidden="true" /> Location</span></div><div className="mockup-info-line" /><div className="mockup-info-line short" /><button type="button" className="mockup-copy-action"><Copy size={14} aria-hidden="true" /> Copy reference</button></>}{variant === "folders" && <><div className="mockup-tabs"><span>Recent</span><span>Starred</span><span className="active">Folders</span></div><div className="folder-list">{[["Saved ideas", "12 items"], ["Places", "6 items"], ["Product details", "8 items"]].map(([name, count]) => <div className="folder-row" key={name}><span className="folder-icon"><Folder size={15} aria-hidden="true" /></span><span><strong>{name}</strong><em>{count}</em></span><MoreHorizontal size={15} aria-hidden="true" /></div>)}</div><div className="mockup-local-note">Stored locally on your device</div></>}</div><div className="floating-bubble" aria-label="Floating bubble example"><Copy size={15} aria-hidden="true" /></div></div>; }
